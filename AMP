train部分可以通过AMP 自动混合精度提高速度，减少内存
对于卷积、池化、全连接层可以采用FP16，loss等精细化部分采用FP32
总的来说，以下部分为代码参考
scaler = torch.amp.GradScaler('cuda')
for epoch in range(CONFIG['epochs']):
        # --- 训练阶段 ---
        model.train()
        epoch_train_loss = 0.0
        for batch_idx, (images, labels) in enumerate(train_loader):
            images, labels = images.to(device), labels.to(device)
            
            optimizer.zero_grad() # 1.零化梯度
            
            # 2 & 3. 开启 autocast 上下文，包含前向传播和计算损失
            with torch.amp.autocast(device_type='cuda'):#自动混合精度，在复杂工作上使用FP16，在简单工作上使用FP32，提升效率同时保持数值稳定
                outputs = model(images) #前向传播
                loss = criterion(outputs, labels)#loss = criterion(outputs, labels) # 3.计算损失  
            scaler.scale(loss).backward()#反向传播，自动缩放损失以适应混合精度训练
            # 5. 更新参数（使用 scaler 自动缩放回来并更新）
            scaler.step(optimizer) #恢复原值更换参数
            
          # 6. 额外步骤：更新缩放器的倍数（为下一个 batch 做准备）
            scaler.update()              
